# flutterSetupScript_for_debian
flutter(dart)の開発環境をdebian(Ubuntuも？)に作成するスクリプト

``` sh
bash -c "$(curl -fsSL https://raw.githubusercontent.com/86chan/flutterSetupScript_for_debian/main/flutterSetUp.sh)"
```
